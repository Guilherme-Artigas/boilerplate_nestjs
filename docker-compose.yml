version: '3.8'

services:

  mysql:
    image: mysql:8
    container_name: mysql
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: senha123
      MYSQL_DATABASE: projeto
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
